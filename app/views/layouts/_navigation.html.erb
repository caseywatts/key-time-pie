<%= link_to "Key Time Pie", root_path, :class => 'brand' %>
<ul class="nav pull-right">
  <% if user_signed_in? %>
    <li class="dropdown">

    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
      <%= current_user.email %>
      <b class="caret"></b>
    </a>

    <ul class="dropdown-menu">
      <li>
      <%= link_to 'Logout', signout_path %>
      </li>
    </ul>

  <% else %>
    <li>
    <%= link_to 'Login', signin_path %>
    </li>
  <% end %>
  </li>
</ul>
